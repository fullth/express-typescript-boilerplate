{
  "name": "express-with-typescript",
  "version": "1.0.0",
  "description": "```shell npm init --yes ``` ### Create a minimal server with Express ```shell npm install express dotenv ``` ### Installing TypeScript ```shell npm i -D typescript @types/express @types/node ``` ### Generating tsconfig.json ```shell npx tsc --init ``` ### Change compilerOptions in tsconfig.json ```shell \"compilerOptions\": {   \"outDir\": \"./dist\" } ``` ### Generate an Express server  ```typescript import express, { Express, Request, Response } from 'express'; import dotenv from 'dotenv';",
  "main": "index.js",
  "scripts": {
    "build": "npx tsc",
    "start": "node dist/index.js",
    "dev": "concurrently \"npx tsc --watch\" \"nodemon -q dist/index.js\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^18.15.10",
    "concurrently": "^7.6.0",
    "nodemon": "^2.0.22",
    "typescript": "^5.0.2"
  }
}
